package team.project.module._template.internal.model.query;

import lombok.Getter;
import lombok.Setter;

/* QO ( Query Object) 查询对象
   用于封装接收上层的请求
   虽然 QO 通常代表查询对象，但这并不意味着它只能用于查询操作
   实际上，QO 可以用于封装任何数据操作的参数和条件，包括增加、删除、修改等操作 */
@Getter
@Setter
public class TmplQO {
}
